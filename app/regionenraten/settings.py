"""
Django settings for regionenraten project.

Generated by 'django-admin startproject' using Django 1.8.

For more information on this file, see
https://docs.djangoproject.com/en/1.8/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/1.8/ref/settings/
"""

# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
import os
import random, string

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

#####################################################################################################################
# Umgebungsvariablen:																								#
#####################################################################################################################
# REGIONENRATEN_DEBUG = "False"										(Default: "True")									#
# REGIONENRATEN_SECRET_KEY = Benutze: https://www.miniwebtool.com/django-secret-key-generator/							#
# REGIONENRATEN_STATIC_ROOT = "/var/www/example.com/static/"		(Default: None)										#
# REGIONENRATEN_STATIC_URL = "/static/"								(Default: "/static/")								#
# REGIONENRATEN_MEDIA_URL = "https://regionenraten.dioe.at/media/"		(Default: "https://regionenraten.dioe.at/media/")		#
# Datenbank:																										#
# REGIONENRATEN_DB="django.db.backends.postgresql"					(Default: "django.db.backends.sqlite3")				#
# REGIONENRATEN_DB_NAME="PersonenDB"								(Default: os.path.join(BASE_DIR, 'db.sqlite3'))		#
# REGIONENRATEN_DB_USER="user"										(Default: None)										#
# REGIONENRATEN_DB_PASSWORD="passwort"								(Default: None)										#
# REGIONENRATEN_DB_HOST="postgresql://localhost"					(Default: None)										#
# REGIONENRATEN_DB_PORT="5433"										(Default: None)										#
#####################################################################################################################

LOGIN_URL = 'regionenraten_login'
LOGOUT_URL = 'regionenraten_logout'
LOGIN_REDIRECT_URL = 'evaluation:start'
CRISPY_TEMPLATE_PACK = 'bootstrap3'

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/1.8/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'yueqlcc54o8+eskilg2=n=#n^i)8o@z_fi7j@$le61xi%2u75z'

# SECURITY WARNING: don't run with debug turned on in production!
if 'REGIONENRATEN_DEBUG' in os.environ and (os.environ['REGIONENRATEN_DEBUG'] == 'False' or os.environ['REGIONENRATEN_DEBUG'] is False):
	DEBUG = False
else:
	DEBUG = True

ALLOWED_HOSTS = []

ALLOWED_SETTINGS_IN_TEMPLATES = ("CACH_RANDOM")

CACH_RANDOM = ''.join(random.SystemRandom().choice(string.ascii_lowercase + string.digits) for i in range(8))

# Application definition

INSTALLED_APPS = (
	'django.contrib.admin',
	'django.contrib.auth',
	'django.contrib.contenttypes',
	'django.contrib.sessions',
	'django.contrib.messages',
	'django.contrib.staticfiles',
	'webpack_loader',
	'crispy_forms',
	'vr',
)

MIDDLEWARE_CLASSES = (
	'django.contrib.sessions.middleware.SessionMiddleware',
	'django.middleware.common.CommonMiddleware',
	'django.middleware.csrf.CsrfViewMiddleware',
	'django.contrib.auth.middleware.AuthenticationMiddleware',
	'django.contrib.auth.middleware.SessionAuthenticationMiddleware',
	'django.contrib.messages.middleware.MessageMiddleware',
	'django.middleware.clickjacking.XFrameOptionsMiddleware',
	'django.middleware.security.SecurityMiddleware',
)

ROOT_URLCONF = 'regionenraten.urls'

TEMPLATES = [
	{
		'BACKEND': 'django.template.backends.django.DjangoTemplates',
		'DIRS': [os.path.join(BASE_DIR, 'regionenraten', 'templates'), os.path.join(BASE_DIR, 'vr', 'templates'), os.path.join(BASE_DIR, 'evaluation', 'templates')],
		'APP_DIRS': True,
		'OPTIONS': {
			'context_processors': [
				'django.template.context_processors.debug',
				'django.template.context_processors.request',
				'django.contrib.auth.context_processors.auth',
				'django.contrib.messages.context_processors.messages',
			],
		},
	},
]

WSGI_APPLICATION = 'regionenraten.wsgi.application'


# Database
# https://docs.djangoproject.com/en/1.8/ref/settings/#databases

DATABASES = {
	'default': {
		'ENGINE': 'django.db.backends.sqlite3',
		'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
	}
}

# Umgebungsvariablen:
if 'REGIONENRATEN_SECRET_KEY' in os.environ:
	SECRET_KEY = os.environ['REGIONENRATEN_SECRET_KEY']

if 'REGIONENRATEN_DB' in os.environ and os.environ['REGIONENRATEN_DB']:
	DATABASES['default']['ENGINE'] = os.environ['REGIONENRATEN_DB']
	if 'REGIONENRATEN_DB_NAME' in os.environ:
		DATABASES['default']['DBNAME'] = os.environ['REGIONENRATEN_DB_NAME']
		DATABASES['default']['NAME'] = os.environ['REGIONENRATEN_DB_NAME']
	if 'REGIONENRATEN_DB_USER' in os.environ:
		DATABASES['default']['USER'] = os.environ['REGIONENRATEN_DB_USER']
	if 'REGIONENRATEN_DB_PASSWORD' in os.environ:
		DATABASES['default']['PASSWORD'] = os.environ['REGIONENRATEN_DB_PASSWORD']
	if 'REGIONENRATEN_DB_HOST' in os.environ:
		DATABASES['default']['HOST'] = os.environ['REGIONENRATEN_DB_HOST']
	if 'REGIONENRATEN_DB_PORT' in os.environ:
		DATABASES['default']['PORT'] = os.environ['REGIONENRATEN_DB_PORT']

# print(DATABASES)

# Internationalization
# https://docs.djangoproject.com/en/1.8/topics/i18n/

LANGUAGE_CODE = 'de-DE'
TIME_ZONE = 'UTC'
USE_I18N = True
USE_L10N = True
USE_TZ = False
USE_THOUSAND_SEPARATOR = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/1.8/howto/static-files/

STATIC_URL = '/static/'

# Umgebungsvariablen:
if 'REGIONENRATEN_STATIC_ROOT' in os.environ and os.environ['REGIONENRATEN_STATIC_ROOT']:
	STATIC_ROOT = os.environ['REGIONENRATEN_STATIC_ROOT']
if 'REGIONENRATEN_STATIC_URL' in os.environ and os.environ['REGIONENRATEN_STATIC_URL']:
	STATIC_URL = os.environ['REGIONENRATEN_STATIC_URL']

STATICFILES_DIRS = (
	os.path.join(BASE_DIR, 'regionenraten', 'static'),
	os.path.join(BASE_DIR, 'evaluation', 'static'),
	os.path.abspath(os.path.join(BASE_DIR, os.pardir, 'webpack_src', 'vr', 'dist')),
)

WEBPACK_LOADER = {
	'VR': {
		'STATS_FILE': os.path.abspath(os.path.join(BASE_DIR, os.pardir, 'webpack_src', 'vr', 'webpack-stats.json')),
	}
}

MEDIA_URL = "https://regionenraten.dioe.at/media/"
MEDIA_DIR = os.path.abspath(os.path.join(BASE_DIR, os.pardir, os.pardir, 'persistent', 'media'))

if 'REGIONENRATEN_MEDIA_URL' in os.environ and os.environ['REGIONENRATEN_MEDIA_URL']:
	MEDIA_URL = os.environ['REGIONENRATEN_MEDIA_URL']
